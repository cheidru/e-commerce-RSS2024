import './profile.scss';
import UserProfile from '../../components/forms/userProfile/userProfile';

function Profile() {
  return (
    <section>
      <UserProfile />
    </section>
  );
}

export default Profile;
